apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sliderule-gcp.fullname" . }}-test-connection"
  labels:
    {{- include "sliderule-gcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sliderule-gcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
