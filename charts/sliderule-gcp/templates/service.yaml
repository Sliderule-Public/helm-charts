apiVersion: v1
kind: Service
metadata:
  name: {{ include "sliderule-gcp.fullname" . }}
  labels:
    {{- include "sliderule-gcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sliderule-gcp.selectorLabels" . | nindent 4 }}
